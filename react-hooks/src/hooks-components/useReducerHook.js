import React from 'react';

export const UseReducerHook = () =>{

    // useReducer is similar to useState, but allows custom state logic 
    // accepts 2 arguments 1- reducer, 2- initialState 
    // it returns current state and dispatch method
}