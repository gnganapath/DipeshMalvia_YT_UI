import React from 'react';

export const UseCallbackHook = () => {

    // 1. Memozie the function ( useCallback ) Vs Memoize the value ( useMemo)
    // 2. Refernential equality for functions

    return (
        <>
            <h5> UseCallback Hooks</h5>
        </>
    )
}